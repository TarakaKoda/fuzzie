import React from "react";

type Props = {};

const Editor = (props: Props) => {
    // Todo: If the use tries to access this route, you should send them to their first workflow they have or send them to create one or you can have your own behavior.
  return <div>Editor</div>;
};

export default Editor;
